import {Component} from '@angular/core';

@Component({
  moduleId: module.id,
  template: `
    <h4>Inconceivable!</h4>
    <div>I don't think this page is where you think it is.</div>
  `
})

export class PageNotFoundComponent { }
